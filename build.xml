<project default="xar" name="sade-textgrid-default-project">
    <property name="build.dir" value="build"/>
    <target name="xar">
        <mkdir dir="${build.dir}"/>
        <zip basedir="." destfile="${build.dir}/sade-textgrid-default-project.xar" excludes="${build.dir},${build.dir}/**"/>
    </target>
</project>
